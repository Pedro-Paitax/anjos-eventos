# Conexão com o banco de dados PostgreSQL
DATABASE_URL=postgresql://anjos:anjos_eventos_2024@localhost:5433/anjos_eventos

# Segredo compartilhado que o webhook do NocoDB deve enviar pra chamar
# POST /api/revalidate (header x-revalidate-secret ou ?secret=). Gere um
# valor aleatório longo (ex.: openssl rand -hex 32) — nunca reutilize um
# valor de exemplo. Ver docs/DECISOES.md, "Cache de Hierarquia_Proteina/
# Macro_Categorias" pra como configurar o webhook do lado do NocoDB.
REVALIDATE_SECRET=
