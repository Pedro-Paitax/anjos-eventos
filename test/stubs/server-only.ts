// Stub de "server-only" para os testes (Vitest/Vite). O pacote real lança
// erro sempre que importado fora do bundler do Next.js — que substitui
// esse módulo por um no-op só na compilação do lado servidor. Ver
// vitest.config.mts (resolve.alias).
export {};
